{
  "name": "kenpom-dashboard",
  "postCreateCommand": "python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -r requirements.txt",
  "postAttachCommand": {
    "server": ".venv/bin/python -m uvicorn app:app --reload --host 0.0.0.0 --port 8000"
  },
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "FastAPI App",
      "onAutoForward": "openPreview"
    }
  }
}